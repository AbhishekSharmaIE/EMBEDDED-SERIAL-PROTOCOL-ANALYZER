/**
 * @file main.c
 * @brief Firmware entry point — project scaffold (protocol simulators to follow).
 */
#include <stdio.h>

int main(void)
{
    (void)printf("protocol_analyzer scaffold\n");
    return 0;
}
