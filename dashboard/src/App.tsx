function App() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>Embedded Serial Protocol Analyzer</h1>
      <p>Dashboard scaffold — protocol UI arrives in a later step.</p>
    </main>
  );
}

export default App;
